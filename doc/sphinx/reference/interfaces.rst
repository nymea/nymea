Available interfaces
====================

This section is generated from the JSON metadata that ships with the nymea
source tree. It lists every interface that can be implemented by a plugin and
links to the JSON description that is consumed by the runtime.

.. include:: generated/interfaces.rst
   :start-after: .. interfaces-begin
